# Infrastructure

Infrastructure for the tafl.online project.

Uses NixOS to declaratively configure the infrastructure, all services, etc.

Deployed using deploy-rs, the command is `deploy .#toph` (the server is named toph)
